{
  "name": "the-open-law-lab-public-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static-first public website for TheOpenLawLab",
  "scripts": {
    "build": "node scripts/fetch-catalogue.mjs && node scripts/build.mjs",
    "serve": "python3 -m http.server 4173 --directory dist"
  },
  "dependencies": {
    "framer-motion": "^12.34.0",
    "lucide-react": "^0.564.0",
    "marked": "^14.1.2",
    "three": "^0.182.0"
  }
}
